<div class="container">
    <nav class="navbar navbar-default navbar-fixed-bottom navbar-inverse" role="navigation">
      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse navbar-ex1-collapse">
        <ul class="nav navbar-nav">
          <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            <%= image_tag "logo/#{params[:id]}.png" %>
          </a>
            <ul class="dropdown-menu">
              <% %w[nfl nhl nba].each do |league| %>
                <% unless params[:id] == league %>
                  <li><%= link_to image_tag("logo/#{league}.png"), score_path(league) %>
                <% end %>
              <% end %>
            </ul>
          </li>
        </ul>
      </div><!-- /.navbar-collapse -->
    </nav>
 </div>
